// app.config.server.ts

import { ApplicationConfig } from '@angular/core';


export const serverConfig: ApplicationConfig = <ApplicationConfig>{
  // Configuration spécifique au rendu côté serveur
  // Par exemple, des fournisseurs de rendu Angular Universal ou d'autres options SSR.
};
